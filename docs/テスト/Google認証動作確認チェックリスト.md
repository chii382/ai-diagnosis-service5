# Google認証動作確認チェックリスト

## 概要

| 項目 | 内容 |
|------|------|
| 対象 | Google認証・会員制機能（AUTH-01） |
| 対象ルート | `/auth/signin` `/dashboard` `/profile` |
| 確認URL | http://localhost:3000 （`npm run dev` で起動） |
| 関連資料 | [AUTH-01 設計書](../tasks/2026-06-10/AUTH-01_Google認証による会員制機能/設計書.md) |

### 事前準備

| 項目 | 内容 |
|------|------|
| 環境変数 | `.env.local` に `AUTH_SECRET` `AUTH_URL` `GOOGLE_CLIENT_ID` `GOOGLE_CLIENT_SECRET` `MONGODB_URI` を設定 |
| Google Cloud | OAuth 同意画面・リダイレクト URI（`http://localhost:3000/api/auth/callback/google`）を設定済み |
| テスト用Googleアカウント | ログイン確認用のアカウントを1つ用意 |
| dev 起動時 | シェル環境変数が `.env.local` を上書きしていないこと（`GOOGLE_CLIENT_ID=test` 等に注意） |

### テスト環境（記入欄）

| 項目 | 内容 |
|------|------|
| 実施日 | YYYY-MM-DD |
| 実施者 | |
| ブラウザ / バージョン | |
| OS | |
| 確認環境 | ローカル / 本番（URL: ） |
| 判定凡例 | ✅=OK / ❌=NG / －=対象外 |

> ❌の場合は「確認結果メモ」欄に事象・再現手順・スクリーンショット名を記入する。

---

## 1. ログインページの表示（/auth/signin）

| # | 確認項目 | 期待結果 | 判定 | メモ |
|---|----------|----------|:----:|------|
| 1-1 | URL直接アクセス | `http://localhost:3000/auth/signin` でログインページが表示される | | |
| 1-2 | ページタイトル | ブラウザタブに「ログイン \| AIキャリア診断」が表示される | | |
| 1-3 | サービス名 | カード上部にコンパスアイコン＋「AIキャリア診断」が表示される | | |
| 1-4 | 見出し | 「会員ログイン」が表示される | | |
| 1-5 | 説明文 | 「Googleアカウントでログインすれば、診断結果とプロフィールを一括で管理できます。」が表示される | | |
| 1-6 | Googleログインボタン | 「Googleでログイン」ボタン（Googleアイコン付き）が表示される | | |
| 1-7 | レイアウト | ダークテーマのカードが画面中央に表示され、横スクロールが発生しない | | |
| 1-8 | LPからの遷移 | LPヘッダー右上「ログイン」クリックで `/auth/signin` に遷移する | | |
| 1-9 | 環境変数未設定時 | 必要な環境変数が未設定の場合、画面上部に警告 Alert が表示される | | |
| 1-10 | 環境変数設定済み時 | 警告 Alert が表示されず、Googleログインボタンが有効（disabled でない） | | |

---

## 2. Googleログインボタンの動作

| # | 確認項目 | 期待結果 | 判定 | メモ |
|---|----------|----------|:----:|------|
| 2-1 | ボタン押下 | 「Googleでログイン」クリックで Google アカウント選択画面（OAuth）に遷移する | | |
| 2-2 | アカウント選択 | テスト用 Google アカウントを選択できる | | |
| 2-3 | ログイン成功 | Google 側の認証完了後、アプリに戻る（エラー画面にならない） | | |
| 2-4 | MongoDB 連携 | 初回ログイン時、MongoDB `users` コレクションにユーザーが作成される | | |
| 2-5 | 再ログイン | 2回目以降のログインも正常に完了する | | |
| 2-6 | 認証エラー時 | 意図的に設定を誤った場合、`error=Configuration` 等のエラーが表示されない（正常設定時） | | |
| 2-7 | ボタン無効時 | 環境変数未設定時、ボタンが disabled でクリックできない | | |

---

## 3. ログイン成功後のリダイレクト

| # | 確認項目 | 期待結果 | 判定 | メモ |
|---|----------|----------|:----:|------|
| 3-1 | 通常ログイン | ログイン成功後、`/dashboard` にリダイレクトされる | | |
| 3-2 | callbackUrl 付き | 未ログイン状態で `/profile` にアクセス → ログイン後、`/profile` にリダイレクトされる | | |
| 3-3 | URL確認 | リダイレクト後、URL が意図したパスになっている | | |
| 3-4 | LPヘッダー | ログイン後、LPヘッダー右上が「ログイン」からアバター＋名前（ダッシュボードリンク）に変わる | | |

---

## 4. ダッシュボードの表示（/dashboard）

| # | 確認項目 | 期待結果 | 判定 | メモ |
|---|----------|----------|:----:|------|
| 4-1 | URL直接アクセス | ログイン済み状態で `/dashboard` が表示される | | |
| 4-2 | ページタイトル | ブラウザタブに「ダッシュボード \| AIキャリア診断」が表示される | | |
| 4-3 | 会員ナビ | 上部に「ダッシュボード」「プロフィール」「ログアウト」ナビが表示される | | |
| 4-4 | ウェルカム | 「ようこそ、{Google表示名} さん」が表示される | | |
| 4-5 | 説明文 | 会員専用エリアの説明・プラン表記（無料プラン等）が表示される | | |
| 4-6 | プロフィールカード | アバター・名前・メールアドレスが Google アカウント情報と一致する | | |
| 4-7 | プロフィール編集リンク | 「プロフィールを編集」ボタンから `/profile` に遷移できる | | |
| 4-8 | クイックリンク | 「プロフィール設定」「トップページ（LP）へ戻る」リンクが動作する | | |
| 4-9 | ナビのアクティブ表示 | ダッシュボードページで「ダッシュボード」ナビがアクティブ表示される | | |

---

## 5. プロフィールページの表示（/profile）

| # | 確認項目 | 期待結果 | 判定 | メモ |
|---|----------|----------|:----:|------|
| 5-1 | URL直接アクセス | ログイン済み状態で `/profile` が表示される | | |
| 5-2 | ページタイトル | ブラウザタブに「プロフィール \| AIキャリア診断」が表示される | | |
| 5-3 | 見出し | 「プロフィール」見出しと説明文が表示される | | |
| 5-4 | ユーザー情報 | アバター・表示名・メールアドレスが正しく表示される | | |
| 5-5 | 登録日・最終更新 | 「登録日」「最終更新」が日時形式で表示される（未設定時は「—」） | | |
| 5-6 | 編集フォーム | 「表示名」入力欄と「メールアドレス」（disabled）が表示される | | |
| 5-7 | メール変更不可 | メール欄に「Google アカウント連携のため変更できません」のヘルパーテキストが表示される | | |
| 5-8 | 保存ボタン | 「変更を保存」ボタンが表示される | | |
| 5-9 | ナビのアクティブ表示 | プロフィールページで「プロフィール」ナビがアクティブ表示される | | |

---

## 6. プロフィール編集の動作

| # | 確認項目 | 期待結果 | 判定 | メモ |
|---|----------|----------|:----:|------|
| 6-1 | 表示名変更 | 表示名を変更して「変更を保存」をクリックできる | | |
| 6-2 | 保存成功 | 「プロフィールを保存しました。」の成功 Alert が表示される | | |
| 6-3 | 画面反映 | 保存後、上部プロフィールカードの表示名が更新される | | |
| 6-4 | 最終更新日 | 「最終更新」日時が更新される | | |
| 6-5 | ダッシュボード連携 | `/dashboard` に戻ると、更新した表示名が反映されている | | |
| 6-6 | 空欄バリデーション | 表示名を空にすると保存ボタンが disabled になる | | |
| 6-7 | 文字数制限 | 50文字を超える入力ができない（maxLength: 50） | | |
| 6-8 | リロード後 | ページをリロードしても変更した表示名が維持される（MongoDB 永続化） | | |
| 6-9 | 未ログインAPI | ログアウト状態で `PUT /api/user/profile` を呼ぶと 401 になる | | |

---

## 7. ログアウト機能

| # | 確認項目 | 期待結果 | 判定 | メモ |
|---|----------|----------|:----:|------|
| 7-1 | ログアウトボタン | 会員ナビの「ログアウト」ボタンが表示される | | |
| 7-2 | ログアウト実行 | 「ログアウト」クリックで `/auth/signin` に遷移する | | |
| 7-3 | セッション解除 | ログアウト後、LPヘッダーが「ログイン」ボタン表示に戻る | | |
| 7-4 | 保護ルート | ログアウト後、`/dashboard` 直接アクセスで `/auth/signin` にリダイレクトされる | | |
| 7-5 | 再ログイン | ログアウト後、再度 Google ログインが正常にできる | | |

---

## 8. 未ログイン時のリダイレクト（保護ルート）

| # | 確認項目 | 期待結果 | 判定 | メモ |
|---|----------|----------|:----:|------|
| 8-1 | `/dashboard` | 未ログインで `/dashboard` にアクセス → `/auth/signin?callbackUrl=/dashboard` にリダイレクト | | |
| 8-2 | `/profile` | 未ログインで `/profile` にアクセス → `/auth/signin?callbackUrl=/profile` にリダイレクト | | |
| 8-3 | callbackUrl | リダイレクト URL に `callbackUrl` クエリが付与されている | | |
| 8-4 | LP（`/`） | 未ログインでも LP トップページは閲覧できる（リダイレクトされない） | | |
| 8-5 | ログインページ | 未ログインでも `/auth/signin` は閲覧できる | | |
| 8-6 | middleware | 保護対象は `/dashboard` `/profile` のみ（上記以外はブロックされない） | | |

---

## 9. セッションの維持

| # | 確認項目 | 期待結果 | 判定 | メモ |
|---|----------|----------|:----:|------|
| 9-1 | ページリロード | ログイン後、`/dashboard` をリロードしてもログイン状態が維持される | | |
| 9-2 | 別ページ遷移 | ダッシュボード ↔ プロフィール ↔ LP を行ってもログイン状態が維持される | | |
| 9-3 | 新規タブ | 同じブラウザで新規タブを開き `/dashboard` にアクセスすると、ログイン状態で表示される | | |
| 9-4 | セッションCookie | 開発者ツール Application → Cookies に NextAuth 関連 Cookie が存在する | | |
| 9-5 | ブラウザ再起動 | ブラウザを一度閉じて再度開き、同一セッション内でログイン状態が維持される（JWT セッション） | | |
| 9-6 | 別ブラウザ | 別ブラウザ（またはシークレット）では未ログイン状態である | | |

---

## 10. その他（品質・コンソール）

| # | 確認項目 | 期待結果 | 判定 | メモ |
|---|----------|----------|:----:|------|
| 10-1 | コンソールエラー | 認証フロー中にブラウザ Console に赤いエラーが出ない | | |
| 10-2 | ネットワーク | `/api/auth/*` `/api/user/profile` が 500 エラーにならない（正常操作時） | | |
| 10-3 | ビルド | `npm run build` がエラーなく完了する | | |
| 10-4 | レスポンシブ | ログイン・ダッシュボード・プロフィールがスマホ幅（375px）でも崩れない | | |

---

## 確認結果サマリ

| カテゴリ | 結果（OK/NG/件数） |
|----------|---------------------|
| 1. ログインページ表示 | |
| 2. Googleログインボタン | |
| 3. ログイン後リダイレクト | |
| 4. ダッシュボード表示 | |
| 5. プロフィールページ表示 | |
| 6. プロフィール編集 | |
| 7. ログアウト | |
| 8. 未ログイン時リダイレクト | |
| 9. セッション維持 | |
| 10. その他品質 | |

**総合判定**：合格 ／ 不合格 ／ 条件付き合格

### 検出した不具合（あれば記載）

| # | 事象 | 再現手順 | 重要度 | 対応状況 |
|---|------|----------|--------|----------|
| | | | | |
